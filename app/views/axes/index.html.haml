%p#notice= notice
%h1 Axes
%table
  %thead
    %tr
      %th Code
      %th Description
      %th{:colspan => "3"}
  %tbody
    - @axes.each do |axis|
      %tr
        %td= axis.code
        %td= axis.description
        %td= link_to 'Show', axis
        %td= link_to 'Edit', edit_axis_path(axis)
        %td= link_to 'Destroy', axis, method: :delete, data: { confirm: 'Are you sure?' }
%br/
= link_to 'New Axis', new_axis_path

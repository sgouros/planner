<%= form_for(act) do |f| %>
  <% if act.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(act.errors.count, "error") %> prohibited this act from being saved:</h2>

      <ul>
      <% act.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :code %>
    <%= f.text_field :code %>
  </div>

  <div class="field">
    <%= f.label :description %>
    <%= f.text_area :description %>
  </div>

  <div class="field">
    <%= f.label :act_plan_id %>
    <%= f.number_field :act_plan_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
